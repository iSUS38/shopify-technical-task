{% schema %}
{
    "name": "t:sections.purchase_event_offer.name",
    "settings": [
        {
            "type": "inline_richtext",
            "label": "Title",
            "id": "t:title"
        }
    ],
    "presets": [
        {
            "name": "t:sections.purchase_event_offer.name"
        }
    ]
}
{% endschema %}