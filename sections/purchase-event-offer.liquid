{% schema %}
{
    "name": "t:sections.purchase_event_offer.name",
    "settings": [
        {
            "type": "inline_richtext",
            "label": "t:title",
            "id": "title"
        }
    ],
    "presets": [
        {
            "name": "t:sections.purchase_event_offer.name"
        }
    ]
}
{% endschema %}